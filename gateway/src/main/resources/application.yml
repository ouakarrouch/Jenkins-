server:
  port: 8888

spring:
  application:
    name: Gateway
  cloud:
    discovery:
      enabled: true

eureka:
  instance:
    hostname: localhost

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    consul:
      enabled: true
  endpoint:
    health:
      show-details: always